// TrustStats — NOT rendered on the homepage.
// Contains fabricated statistics (99.8% accuracy, 120K users, 15.2M words) with no verifiable evidence.
// Kept in source only to preserve file history; do not re-import in any page.
export default function TrustStats() {
  return null;
}
